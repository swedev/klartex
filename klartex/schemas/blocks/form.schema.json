{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://klartex.se/schemas/blocks/form/v0.1",
  "title": "Form Block",
  "description": "Label/value rows for paper-signed agreements. Each field has a label; if value is missing the row renders as a horizontal rule for handwritten input. Compose with heading and columns blocks for richer layouts.",
  "type": "object",
  "required": ["type", "fields"],
  "additionalProperties": false,
  "properties": {
    "type": { "type": "string", "const": "form" },
    "fields": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["label"],
        "additionalProperties": false,
        "properties": {
          "label": {
            "type": "string",
            "description": "Field name shown in bold, followed by a colon."
          },
          "value": {
            "type": "string",
            "description": "Pre-filled value. If omitted or empty, the row renders a blank rule for handwritten input."
          }
        }
      }
    }
  }
}
