{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://klartex.se/schemas/block-engine/v0.2",
  "title": "Block Engine Document",
  "description": "Universal block-based document format. The agent composes body[] freely from typed blocks.",
  "type": "object",
  "required": ["body"],
  "additionalProperties": false,
  "properties": {
    "page_template": {
      "oneOf": [
        {
          "type": "string",
          "description": "Page template name (e.g. 'formal', 'clean', 'none')"
        },
        {
          "type": "object",
          "required": ["name"],
          "properties": {
            "name": { "type": "string", "description": "Page template name" },
            "page_numbers": { "type": "boolean", "description": "Override: show page numbers" },
            "first_page_header": { "type": "boolean", "description": "Override: show header on first page" },
            "font": { "type": "string", "description": "Dokumentets typsnitt (fontspec-namn, t.ex. 'Futura'). Måste finnas installerat där renderingen sker." },
            "header_font": { "type": "string", "description": "Typsnitt för sidhuvud och sidfot. Default: samma som font." },
            "diff_highlight": { "type": "string", "enum": ["none", "added", "removed", "both"], "default": "none", "description": "Bakgrundsfärg bakom ändringsmarkerad text ({+tillagt+} / [-struket-]): \"added\", \"removed\", \"both\" eller \"none\". Struken text med bakgrund saknar genomstrykning \u2014 de går inte att kombinera." },
            "footer": {
              "type": "object",
              "additionalProperties": false,
              "description": "Sidfot på varje sida med företags-, kontakt- och betalningsuppgifter.",
              "properties": {
                "company": { "type": "string", "description": "Företagsnamn" },
                "address": { "oneOf": [{ "type": "string" }, { "type": "array", "items": { "type": "string" } }], "description": "Postadress som array med en rad per element, t.ex. ['Storgatan 1', '123 45 Stad'] — radbryts som en postadress. En sträng renderas som en enda rad." },
                "seat": { "type": "string", "description": "Styrelsens säte, t.ex. 'Malmö'" },
                "org_number": { "type": "string", "description": "Organisationsnummer" },
                "vat_number": { "type": "string", "description": "Momsregistreringsnummer (SExxxxxxxxxx01)" },
                "f_tax": { "type": "boolean", "description": "true visar raden 'Godkänd för F-skatt'" },
                "phone": { "type": "string", "description": "Telefon" },
                "email": { "type": "string", "description": "E-post" },
                "web": { "type": "string", "description": "Webbplats" },
                "bankgiro": { "type": "string" },
                "plusgiro": { "type": "string" },
                "iban": { "type": "string" },
                "bic": { "type": "string" }
              }
            }
          },
          "additionalProperties": false
        }
      ],
      "default": "none"
    },
    "lang": {
      "type": "string",
      "enum": ["sv", "en"],
      "default": "sv",
      "description": "Document language"
    },
    "block_settings": {
      "type": "object",
      "description": "Document-wide spacing overrides keyed by block type name (e.g. 'heading', 'table'). Applies to every instance of that block type in the document; a per-instance spacing_before/spacing_after field on a block wins over this. Supported types: heading, text, list, table, callout, quote, description_list.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "spacing_before": {
            "type": "string",
            "description": "Vertical space above each instance. Plain-unit LaTeX dimension string ('1em', '0.5cm', '0em'). Macro-based dimensions ('\\baselineskip') are not supported — backslashes are escaped like in all other strings. Not validated beyond that — an invalid dimension fails at compile time."
          },
          "spacing_after": {
            "type": "string",
            "description": "Vertical space below each instance. Same semantics as spacing_before."
          }
        }
      }
    },
    "body": {
      "type": "array",
      "description": "Ordered list of document blocks",
      "items": {
        "type": "object",
        "required": ["type"],
        "properties": {
          "type": {
            "type": "string",
            "description": "Block type name"
          }
        }
      },
      "minItems": 1
    }
  }
}
