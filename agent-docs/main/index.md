# Main Branch

**Branch:** main

## Overview

Klartex is an open source PDF generation service: structured JSON data + template name + branding → professional PDF via XeLaTeX.

Three consumption modes: Python library, CLI tool, HTTP service (FastAPI).
Three templates: protokoll (meeting minutes), faktura (invoice), avtal (agreement).
Dynamic branding via external YAML config with configurable header layouts.

## Context Files

(Add project-brief.md, tech-context.md, system-patterns.md as the project matures)
