{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://klartex.se/schemas/blocks/clause/v0.1",
  "title": "Clause Block",
  "description": "Numbered legal clause with title and items",
  "type": "object",
  "required": ["type", "title", "items"],
  "additionalProperties": false,
  "properties": {
    "type": { "type": "string", "const": "clause" },
    "title": { "type": "string", "description": "Clause title" },
    "number": {
      "type": "integer",
      "minimum": 1,
      "description": "Explicit clause number. When set, the clause renders as '§ N. Title' and sub-items use 'N.1, N.2, …' regardless of position in body[]. When absent, the clause is auto-numbered as 'N. Title' (no §)."
    },
    "items": {
      "type": "array",
      "description": "Clause items (strings or nested objects with sub-items)",
      "items": {
        "oneOf": [
          { "type": "string" },
          {
            "type": "object",
            "required": ["items"],
            "anyOf": [
              { "required": ["title"] },
              { "required": ["text"] }
            ],
            "properties": {
              "title": { "type": "string" },
              "text": { "type": "string" },
              "list": { "type": "string", "enum": ["numbered", "bullet"], "default": "numbered" },
              "items": { "type": "array", "minItems": 1 }
            }
          }
        ]
      },
      "minItems": 1
    }
  }
}
