{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Kvitto",
  "description": "Kvitto / Receipt",
  "type": "object",
  "required": ["receipt_number", "date", "total_amount", "items"],
  "properties": {
    "page_template": {
      "$comment": "Generated at load time from the slot model in klartex/page_templates.py (page_template_schema); see registry.py."
    },
    "number_format": { "type": "string", "enum": ["sv", "en"], "description": "Number format for amounts: 'sv' gives 32 400,00 (default), 'en' gives 32,400.00." },
    "logo": { "type": "string", "description": "Image file (pdf/png) shown in the receipt's top-left corner, aligned with the KVITTO block. A plain file name is looked up via the asset directory (the page template's directory) and cwd." },
    "logo_height": { "type": "string", "default": "1cm", "description": "Logo height as a LaTeX dimension, e.g. '1.5cm'." },
    "logo_offset": {
      "type": "object",
      "additionalProperties": false,
      "description": "Fine-tunes the logo position, as fractions of its height: positive x moves right (negative left), positive y moves up (negative down). Starting point: left edge at the margin, top edge aligned with the KVITTO heading.",
      "properties": {
        "x": { "type": "number", "default": 0 },
        "y": { "type": "number", "default": 0 }
      }
    },
    "sender": {
      "type": "object",
      "required": ["name"],
      "description": "Issuer/seller — shown as the Avsändare block in the receipt body. Give the company's legal name here even when a logo or a page-template footer is present.",
      "properties": {
        "name": { "type": "string" },
        "org_number": { "type": "string" },
        "address_line1": { "type": "string" },
        "address_line2": { "type": "string" }
      }
    },
    "footer": { "not": { "description": "`footer` is not a property of kvitto. Company, contact and payment details go in the page template's footer slot: `page_template.footer` with variant `columns` and its `fields`." } },
    "receipt_number": { "type": "string" },
    "date": { "type": "string", "description": "Receipt date (YYYY-MM-DD)" },
    "total_amount": { "type": "number", "description": "Total amount paid. Authoritative — never computed from items." },
    "currency": { "type": "string", "default": "SEK" },
    "payment_method": { "type": "string", "description": "e.g. Kontant, Swish, Kort" },
    "paid_by": { "type": "string", "description": "Name of the payer" },
    "items": {
      "type": "array",
      "description": "What was paid for. Amounts are optional and informational — total_amount is the authoritative total.",
      "items": {
        "type": "object",
        "required": ["description"],
        "properties": {
          "description": { "type": "string" },
          "amount": { "type": "number" }
        }
      },
      "minItems": 1
    },
    "note": { "type": "string", "description": "Optional note shown below the receipt content." }
  }
}
